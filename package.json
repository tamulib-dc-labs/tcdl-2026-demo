{
  "name": "whisper-transcript-reviewer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Review VTTs Generated by Whisper or Whisper X.",
  "scripts": {
    "setup": "node scripts/setup.js",
    "build:config": "node scripts/build-config.js",
    "dev": "npm run build:config && vite",
    "build": "npm run build:config && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "yaml": "^2.7.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.16",
    "tailwindcss": "^4.0.16",
    "whisper-transcript-sticky": "^0.1.0"
  }
}
